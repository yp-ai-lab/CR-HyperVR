steps:
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', '${_IMAGE}', '.']
images:
  - '${_IMAGE}'
substitutions:
  _IMAGE: '$REGION-docker.pkg.dev/$PROJECT_ID/embedding-service/api:latest'

